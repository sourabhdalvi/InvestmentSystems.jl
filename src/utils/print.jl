# function Base.summary(ist::InvestmentComponent)
#     # All InfrastructureSystemsType subtypes are supposed to implement get_name.
#     # Some don't.  They need to override this function.
#     return "($(typeof(ist)))"
# end

# function Base.summary(ist::InvestmentComponent)
#     # All InfrastructureSystemsType subtypes are supposed to implement get_name.
#     # Some don't.  They need to override this function.
#     return "($(typeof(ist)))"
# end
